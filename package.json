{
  "name": "my_little_store-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --host 0.0.0.0",
    "dev": "json-server --watch db.json --port 10000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
